# ==================================
# DATABASE CONFIGURATION
# ==================================

# PostgreSQL database host (use 'localhost' for local development)
DB_HOST=localhost

# PostgreSQL database port (default: 5432)
DB_PORT=5432

# PostgreSQL database username
DB_USERNAME=postgres

# PostgreSQL database password
DB_PASSWORD=your_password_here

# PostgreSQL database name
DB_NAME=whatsapp_builder

# TypeORM synchronize (ALWAYS false - use migrations)
DB_SYNCHRONIZE=false

# TypeORM logging (true for development, false for production)
DB_LOGGING=false

# ==================================
# APPLICATION CONFIGURATION
# ==================================

# Port the backend server will run on (default: 3000)
PORT=3000

# Node environment (development, production, test)
NODE_ENV=development

# ==================================
# WHATSAPP BUSINESS API
# ==================================

# WhatsApp Business API access token
# Get this from Meta Business Manager > WhatsApp > API Setup
WHATSAPP_API_TOKEN=your_whatsapp_api_token_here

# WhatsApp Phone Number ID (not the phone number itself)
# Get this from Meta Business Manager > WhatsApp > API Setup
WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id_here

# WhatsApp Business Account ID (optional, for certain operations)
WHATSAPP_BUSINESS_ACCOUNT_ID=your_business_account_id_here

# WhatsApp API version (e.g., v21.0, v22.0)
# Check latest version at: https://developers.facebook.com/docs/whatsapp/changelog
WHATSAPP_API_VERSION=v21.0

# ==================================
# WHATSAPP WEBHOOK CONFIGURATION
# ==================================

# App Secret for webhook signature verification
# Get this from Meta App Dashboard > Settings > Basic > App Secret
WHATSAPP_APP_SECRET=your_app_secret_from_meta_dashboard

# Webhook verify token (you create this)
# Use a secure random string. You'll need to enter this when configuring webhooks
# in Meta Business Manager
WHATSAPP_WEBHOOK_VERIFY_TOKEN=your_custom_verify_token_12345

# ==================================
# FRONTEND CONFIGURATION
# ==================================

# Frontend URL for CORS configuration
# For local development, this is the Vite dev server (default port: 5173)
# For production, use your deployed frontend URL
FRONTEND_URL=http://localhost:5173

# ==================================
# OPTIONAL CONFIGURATION
# ==================================

# Logging level (error, warn, info, debug, verbose)
# Use 'debug' for development, 'info' or 'warn' for production
LOG_LEVEL=debug

# JWT Secret (if implementing authentication)
# JWT_SECRET=your_jwt_secret_here

# JWT Expiration (if implementing authentication)
# JWT_EXPIRATION=7d

# ==================================
# DEVELOPMENT TOOLS
# ==================================

# ngrok authtoken (optional, for webhook development)
# Get this from: https://dashboard.ngrok.com/get-started/your-authtoken
# NGROK_AUTHTOKEN=your_ngrok_authtoken_here

# ==================================
# GOOGLE OAUTH CONFIGURATION
# ==================================

# Google OAuth Client ID
# Get this from Google Cloud Console > APIs & Services > Credentials
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com

# Google OAuth Client Secret
# Get this from Google Cloud Console > APIs & Services > Credentials
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Google OAuth Redirect URI
# Must match the redirect URI configured in Google Cloud Console
GOOGLE_REDIRECT_URI=http://localhost:3000/api/google-oauth/callback

# ==================================
# NOTES
# ==================================

# 1. Copy this file to .env and fill in your actual values
# 2. Never commit .env to version control
# 3. .env is already in .gitignore
# 4. All team members should have their own .env file
# 5. For production, set these as environment variables in your hosting platform
# 6. The FRONTEND_URL should be http://localhost:5173 for local development (Vite's default port)
#    NOT http://localhost:3000 (which is the backend port)
